{
  "name": "nuxis",
  "version": "0.0.0",
  "description": "An extractor based backend framework",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "tsc -p test/tsconfig.json && node --experimental-test-coverage --test-reporter=spec --enable-source-maps --test"
  },
  "type": "module",
  "devDependencies": {},
  "dependencies": {
    "path-to-regexp": "^6.2.1"
  }
}
